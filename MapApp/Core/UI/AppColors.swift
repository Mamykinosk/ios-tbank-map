import SwiftUI

extension Color {
    // Base
    static let appBackground = Color(red: 252 / 255, green: 249 / 255, blue: 244 / 255)
    static let appPrimary = Color(red: 22 / 255, green: 52 / 255, blue: 41 / 255)
    static let appSecondary = Color(red: 65 / 255, green: 72 / 255, blue: 69 / 255)
    static let appFieldBackground = Color(red: 235 / 255, green: 232 / 255, blue: 227 / 255)
    static let appAccent = Color(red: 45 / 255, green: 75 / 255, blue: 63 / 255)
    static let appMuted = Color(red: 229 / 255, green: 226 / 255, blue: 221 / 255)

    // Frequently used text / icons
    static let appTitle = Color(red: 28 / 255, green: 28 / 255, blue: 25 / 255)
    static let appPlaceholder = Color(red: 168 / 255, green: 162 / 255, blue: 158 / 255)
    static let appSecondaryAction = Color(red: 67 / 255, green: 102 / 255, blue: 77 / 255)

    // Repeated decorative colors
    static let appGreenGlow = Color(red: 194 / 255, green: 233 / 255, blue: 201 / 255)
    static let appBlueGlow = Color(red: 189 / 255, green: 233 / 255, blue: 255 / 255)

    // Optional shared status colors
    static let appError = Color(red: 186 / 255, green: 26 / 255, blue: 26 / 255)
    static let appErrorBackground = Color(red: 254 / 255, green: 237 / 255, blue: 237 / 255)
    static let appSuccessBackground = Color(red: 235 / 255, green: 247 / 255, blue: 238 / 255)
}
